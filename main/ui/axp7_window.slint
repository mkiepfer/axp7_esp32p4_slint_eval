import { HomeScreen } from "home.slint";
import { ScrollView, TabWidget } from "std-widgets.slint";
import { CleaningScreen } from "cleaning.slint";

export component AppWindow inherits Window {
    title: "AXP7 Showcase";

    TabWidget {
        //width: 1024px;
        //height: 600px;

        Tab {
            home := HomeScreen {
                width: 1024px;
                height: 600px;
            }
        }

        Tab {
            cleaning := CleaningScreen {
                width: 1024px;
                height: 600px;
                //x:1024px;
            }
        }
    }
}
